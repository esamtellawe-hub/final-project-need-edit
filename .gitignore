# Dependencies
node_modules/
package-lock.json  # (اختياري، يفضل رفعه لتوحيد النسخ، لكن node_modules ممنوع)
yarn.lock

# Environment Variables (سري جداً!)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Build Output
dist/
build/

# Uploads (عشان ما ترفع صور المستخدمين وتثقل الموقع)
uploads/
backend/uploads/
server/uploads/

# OS Files
.DS_Store
Thumbs.db

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*